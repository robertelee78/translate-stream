# Audio transcription and translation dependencies
# 
# IMPORTANT: System dependencies required before installing:
# 
# Ubuntu/Debian:
#   sudo apt-get install portaudio19-dev python3-dev python3-pyaudio ffmpeg
#
# macOS:
#   brew install portaudio ffmpeg
#
# Windows:
#   - Install FFmpeg from https://ffmpeg.org/download.html
#   - PyAudio wheels should install automatically
#
# If PyAudio installation fails, try:
#   Ubuntu/Debian: sudo apt-get install python3-pyaudio
#   macOS: pip install --global-option='build_ext' --global-option='-I/usr/local/include' --global-option='-L/usr/local/lib' pyaudio
#

websockets>=12.0
aiohttp>=3.9.0
pyaudio>=0.2.13
numpy>=1.24.0
python-dotenv>=1.0.0
click>=8.0.0
pydub>=0.25.1
openai>=1.0.0
nest-asyncio>=1.5.0
kivy>=2.2.0